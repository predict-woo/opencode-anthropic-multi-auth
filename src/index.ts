import type { Plugin } from "@opencode-ai/plugin";

// Implementation in Task N
export const AnthropicMultiAuthPlugin: Plugin = async ({ client }) => {
  return {};
};

export default AnthropicMultiAuthPlugin;
